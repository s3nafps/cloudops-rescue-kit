name: Shell syntax

on:
  push:
  pull_request:

jobs:
  bash-syntax:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check Bash scripts
        run: |
          for file in scripts/*.sh; do
            bash -n "$file"
          done
